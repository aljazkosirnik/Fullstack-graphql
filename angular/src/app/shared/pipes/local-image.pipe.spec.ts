import { LocalImagePipe } from './local-image.pipe';

describe('LocalImagePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalImagePipe();
    expect(pipe).toBeTruthy();
  });
});
